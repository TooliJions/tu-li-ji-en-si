# P1.1 分析面板真实化 · Telemetry 子系统与 LLM 灵感洗牌实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Studio `/analytics` 的 `token-usage` 与 `inspiration-shuffle` 两个端点返回真实数据：在 core 内落盘按章节、按五大通道（writer/auditor/planner/composer/reviser）的 token 用量；通过 core-bridge 将 LLMProvider 暴露给 Studio 用于 3 路并行风格改写。

**Architecture:** 在 core 新增 `TelemetryLogger`，以 `<root>/<bookId>/story/state/telemetry/chapter-<padded>.json` 为单章遥测文件；`PipelineRunner` 在 writeNextChapter/writeDraft 各阶段收集 usage 并调用 logger。Studio core-bridge 新增 `getStudioLLMProvider()` 对外暴露 provider 句柄；analytics 路由读取遥测文件做聚合，灵感洗牌改为 3 次并行 provider 调用。

**Tech Stack:** TypeScript / pnpm workspace / Vitest / Hono / SQLite WAL / Zod。

---

## 文件结构一览

**新建：**
- `packages/core/src/telemetry/logger.ts` — `TelemetryLogger` 实现
- `packages/core/src/telemetry/logger.test.ts` — 单元测试

**修改：**
- `packages/core/src/index.ts` — 导出 telemetry 模块
- `packages/core/src/pipeline/runner.ts` — writeNextChapter/writeDraft 集成 telemetry
- `packages/core/src/pipeline/runner.test.ts` — 增加遥测落盘断言
- `packages/studio/src/api/core-bridge.ts` — 新增 `getStudioLLMProvider()`
- `packages/studio/src/api/routes/analytics.ts` — token-usage / inspiration-shuffle 真实化
- `packages/studio/src/api/routes/analytics.test.ts` — 补真实化断言
- `packages/studio/src/pages/analytics.tsx` — `TokenUsageData` 类型对齐

---

## Task 1：TelemetryLogger 骨架与 record API（TDD）

**Files:**
- Create: `packages/core/src/telemetry/logger.ts`
- Create: `packages/core/src/telemetry/logger.test.ts`

- [ ] **Step 1：写失败测试**

创建 `packages/core/src/telemetry/logger.test.ts`：

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import * as fs from 'node:fs';
import * as os from 'node:os';
import * as path from 'node:path';
import { TelemetryLogger } from './logger';

describe('TelemetryLogger', () => {
  let root: string;

  beforeEach(() => {
    root = fs.mkdtempSync(path.join(os.tmpdir(), 'cn-telemetry-'));
    fs.mkdirSync(path.join(root, 'book-001', 'story', 'state'), { recursive: true });
  });

  it('record() 首次写入时创建文件并返回累计结果', () => {
    const logger = new TelemetryLogger(root);
    const result = logger.record('book-001', 1, 'writer', {
      promptTokens: 100,
      completionTokens: 50,
      totalTokens: 150,
    });

    expect(result.bookId).toBe('book-001');
    expect(result.chapterNumber).toBe(1);
    expect(result.channels.writer.totalTokens).toBe(150);
    expect(result.channels.writer.calls).toBe(1);
    expect(result.totalTokens).toBe(150);

    const filePath = path.join(root, 'book-001', 'story', 'state', 'telemetry', 'chapter-0001.json');
    expect(fs.existsSync(filePath)).toBe(true);
  });

  it('record() 多次累加同一通道', () => {
    const logger = new TelemetryLogger(root);
    logger.record('book-001', 1, 'writer', { promptTokens: 100, completionTokens: 50, totalTokens: 150 });
    const result = logger.record('book-001', 1, 'writer', {
      promptTokens: 200,
      completionTokens: 80,
      totalTokens: 280,
    });

    expect(result.channels.writer.totalTokens).toBe(430);
    expect(result.channels.writer.calls).toBe(2);
    expect(result.totalTokens).toBe(430);
  });

  it('record() 多通道累加独立', () => {
    const logger = new TelemetryLogger(root);
    logger.record('book-001', 1, 'writer', { promptTokens: 100, completionTokens: 50, totalTokens: 150 });
    logger.record('book-001', 1, 'auditor', { promptTokens: 60, completionTokens: 30, totalTokens: 90 });
    const result = logger.record('book-001', 1, 'reviser', {
      promptTokens: 40,
      completionTokens: 20,
      totalTokens: 60,
    });

    expect(result.channels.writer.totalTokens).toBe(150);
    expect(result.channels.auditor.totalTokens).toBe(90);
    expect(result.channels.reviser.totalTokens).toBe(60);
    expect(result.channels.planner.totalTokens).toBe(0);
    expect(result.totalTokens).toBe(300);
  });

  it('read() 对不存在章节返回 null', () => {
    const logger = new TelemetryLogger(root);
    expect(logger.read('book-001', 99)).toBeNull();
  });

  it('read() 返回已写入章节', () => {
    const logger = new TelemetryLogger(root);
    logger.record('book-001', 2, 'planner', { promptTokens: 30, completionTokens: 10, totalTokens: 40 });
    const got = logger.read('book-001', 2);
    expect(got).not.toBeNull();
    expect(got!.channels.planner.totalTokens).toBe(40);
  });

  it('listBookTelemetry() 返回该书所有章节遥测', () => {
    const logger = new TelemetryLogger(root);
    logger.record('book-001', 1, 'writer', { promptTokens: 10, completionTokens: 5, totalTokens: 15 });
    logger.record('book-001', 2, 'writer', { promptTokens: 20, completionTokens: 10, totalTokens: 30 });
    logger.record('book-001', 3, 'writer', { promptTokens: 30, completionTokens: 15, totalTokens: 45 });

    const all = logger.listBookTelemetry('book-001');
    expect(all.length).toBe(3);
    expect(all.map((x) => x.chapterNumber)).toEqual([1, 2, 3]);
  });

  it('listBookTelemetry() 对空 book 返回 []', () => {
    const logger = new TelemetryLogger(root);
    expect(logger.listBookTelemetry('book-empty')).toEqual([]);
  });
});
```

- [ ] **Step 2：运行测试确认失败**

Run: `pnpm --filter @cybernovelist/core test -- telemetry/logger`

Expected: FAIL — `Cannot find module './logger'`

- [ ] **Step 3：实现 TelemetryLogger**

创建 `packages/core/src/telemetry/logger.ts`：

```typescript
import * as fs from 'node:fs';
import * as path from 'node:path';

export type TelemetryChannel = 'writer' | 'auditor' | 'planner' | 'composer' | 'reviser';

export interface ChannelUsage {
  promptTokens: number;
  completionTokens: number;
  totalTokens: number;
  calls: number;
}

export interface ChapterTelemetry {
  bookId: string;
  chapterNumber: number;
  channels: Record<TelemetryChannel, ChannelUsage>;
  totalTokens: number;
  createdAt: string;
  updatedAt: string;
}

const CHANNELS: TelemetryChannel[] = ['writer', 'auditor', 'planner', 'composer', 'reviser'];

export class TelemetryLogger {
  private rootDir: string;

  constructor(rootDir: string) {
    this.rootDir = rootDir;
  }

  record(
    bookId: string,
    chapterNumber: number,
    channel: TelemetryChannel,
    usage: { promptTokens: number; completionTokens: number; totalTokens: number },
  ): ChapterTelemetry {
    const existing = this.read(bookId, chapterNumber);
    const now = new Date().toISOString();

    const telemetry: ChapterTelemetry = existing ?? {
      bookId,
      chapterNumber,
      channels: this.emptyChannels(),
      totalTokens: 0,
      createdAt: now,
      updatedAt: now,
    };

    const entry = telemetry.channels[channel];
    entry.promptTokens += usage.promptTokens;
    entry.completionTokens += usage.completionTokens;
    entry.totalTokens += usage.totalTokens;
    entry.calls += 1;

    telemetry.totalTokens = CHANNELS.reduce((sum, ch) => sum + telemetry.channels[ch].totalTokens, 0);
    telemetry.updatedAt = now;

    this.writeFile(bookId, chapterNumber, telemetry);
    return telemetry;
  }

  read(bookId: string, chapterNumber: number): ChapterTelemetry | null {
    const filePath = this.getChapterPath(bookId, chapterNumber);
    if (!fs.existsSync(filePath)) return null;
    return JSON.parse(fs.readFileSync(filePath, 'utf-8')) as ChapterTelemetry;
  }

  listBookTelemetry(bookId: string): ChapterTelemetry[] {
    const dir = path.join(this.rootDir, bookId, 'story', 'state', 'telemetry');
    if (!fs.existsSync(dir)) return [];

    const files = fs
      .readdirSync(dir)
      .filter((name) => /^chapter-\d+\.json$/.test(name))
      .sort();

    return files
      .map((name) => JSON.parse(fs.readFileSync(path.join(dir, name), 'utf-8')) as ChapterTelemetry)
      .sort((a, b) => a.chapterNumber - b.chapterNumber);
  }

  private getChapterPath(bookId: string, chapterNumber: number): string {
    const padded = String(chapterNumber).padStart(4, '0');
    return path.join(this.rootDir, bookId, 'story', 'state', 'telemetry', `chapter-${padded}.json`);
  }

  private emptyChannels(): Record<TelemetryChannel, ChannelUsage> {
    const init = {} as Record<TelemetryChannel, ChannelUsage>;
    for (const ch of CHANNELS) {
      init[ch] = { promptTokens: 0, completionTokens: 0, totalTokens: 0, calls: 0 };
    }
    return init;
  }

  private writeFile(bookId: string, chapterNumber: number, telemetry: ChapterTelemetry): void {
    const filePath = this.getChapterPath(bookId, chapterNumber);
    fs.mkdirSync(path.dirname(filePath), { recursive: true });
    fs.writeFileSync(filePath, JSON.stringify(telemetry, null, 2), 'utf-8');
  }
}
```

- [ ] **Step 4：运行测试确认通过**

Run: `pnpm --filter @cybernovelist/core test -- telemetry/logger`

Expected: PASS — 全部 7 条测试通过。

- [ ] **Step 5：提交**

```bash
git add packages/core/src/telemetry/logger.ts packages/core/src/telemetry/logger.test.ts
git commit -m "feat(core): add TelemetryLogger for per-chapter token usage tracking"
```

---

## Task 2：从 core 导出 TelemetryLogger

**Files:**
- Modify: `packages/core/src/index.ts`

- [ ] **Step 1：追加导出**

在 `packages/core/src/index.ts` 末尾追加一行：

```typescript
export * from './telemetry/logger';
```

- [ ] **Step 2：验证 typecheck**

Run: `pnpm --filter @cybernovelist/core typecheck`

Expected: 无错误。

- [ ] **Step 3：提交**

```bash
git add packages/core/src/index.ts
git commit -m "chore(core): export TelemetryLogger from public API"
```

---

## Task 3：PipelineRunner 集成 telemetry（writeNextChapter 路径）

**Files:**
- Modify: `packages/core/src/pipeline/runner.ts`

- [ ] **Step 1：在 PipelineConfig 接受可选 telemetryLogger 并在构造器初始化**

打开 [packages/core/src/pipeline/runner.ts](packages/core/src/pipeline/runner.ts)，在顶部引入：

```typescript
import { TelemetryLogger, type TelemetryChannel } from '../telemetry/logger';
```

在 `PipelineConfig` 接口（约第 12 行）追加字段：

```typescript
export interface PipelineConfig {
  rootDir: string;
  llmConfig?: LLMConfig;
  provider?: LLMProvider;
  maxRevisionRetries?: number;
  fallbackAction?: 'accept_with_warnings' | 'pause';
  telemetryLogger?: TelemetryLogger;
}
```

在 `PipelineRunner` 类字段区（约第 96 行附近）新增：

```typescript
  private telemetryLogger: TelemetryLogger;
```

在构造器末尾加入：

```typescript
    this.telemetryLogger = config.telemetryLogger ?? new TelemetryLogger(config.rootDir);
```

- [ ] **Step 2：在 PipelineRunner 类内加入 #trackUsage 私有方法**

在类内任何 `#` 私有方法区域（例如 `#extractMemory` 附近）加入：

```typescript
  #trackUsage(
    bookId: string,
    chapterNumber: number,
    channel: TelemetryChannel,
    usage: { promptTokens: number; completionTokens: number; totalTokens: number } | undefined,
  ): void {
    if (!usage) return;
    this.telemetryLogger.record(bookId, chapterNumber, channel, usage);
  }
```

- [ ] **Step 3：在 writeNextChapter 流水线按通道打点**

找到 `writeNextChapter` 方法中 `#generateIntent`、`#buildContextCard`、`#generateDraft`、`#polishScene`、`#auditAndRevise` 被调用的位置（约第 260–300 行）。每次调用后追加一次 `#trackUsage`。

示意改造（以 `#generateDraft` 为例，其他四处同理，planner 通道对应 intent + contextCard，composer 对应 polished，auditor 与 reviser 需拆分，见 Step 4）：

```typescript
      const draft = await this.#generateDraft(input, meta.genre, contextCard, intent);
      this.#trackUsage(input.bookId, input.chapterNumber, 'writer', draft.usage);
      if (!draft.success) { ... }

      const polished = await this.#polishScene(draft.content!, meta.genre, input.chapterNumber);
      this.#trackUsage(input.bookId, input.chapterNumber, 'composer', polished.usage);
```

对于 `intent` 与 `contextCard`，需确保这两个私有方法返回 `usage` 字段，若当前未返回请在 Step 4 处补。

- [ ] **Step 4：改造 #generateIntent / #buildContextCard / #auditAndRevise 返回分离 usage**

重点改造 `#auditAndRevise`，让其返回 auditor 与 reviser 分离的 usage：

```typescript
  async #auditAndRevise(
    chapterContent: string,
    input: WriteNextChapterInput,
    genre: string
  ): Promise<{
    content: string;
    auditorUsage?: { promptTokens: number; completionTokens: number; totalTokens: number };
    reviserUsage?: { promptTokens: number; completionTokens: number; totalTokens: number };
    usage?: AgentResult['usage'];
    warning?: string;
    warningCode?: 'accept_with_warnings';
  }> {
    let currentContent = chapterContent;
    const auditorUsage = { promptTokens: 0, completionTokens: 0, totalTokens: 0 };
    const reviserUsage = { promptTokens: 0, completionTokens: 0, totalTokens: 0 };

    for (let attempt = 0; attempt <= this.maxRevisionRetries; attempt++) {
      const auditResult = await this.#auditChapter(currentContent, genre, input.chapterNumber);
      if (auditResult.usage) {
        auditorUsage.promptTokens += auditResult.usage.promptTokens;
        auditorUsage.completionTokens += auditResult.usage.completionTokens;
        auditorUsage.totalTokens += auditResult.usage.totalTokens;
      }

      if (auditResult.status === 'pass' || auditResult.issues.length === 0) {
        return {
          content: currentContent,
          auditorUsage,
          reviserUsage,
          usage: {
            promptTokens: auditorUsage.promptTokens + reviserUsage.promptTokens,
            completionTokens: auditorUsage.completionTokens + reviserUsage.completionTokens,
            totalTokens: auditorUsage.totalTokens + reviserUsage.totalTokens,
          },
        };
      }

      if (attempt < this.maxRevisionRetries) {
        const revisePrompt = `请根据以下审计问题修订章节内容：

## 审计问题
${auditResult.issues.map((i: { severity: string; description: string }) => `- [${i.severity}] ${i.description}`).join('\n')}

## 当前内容
${currentContent}

请修订后输出完整正文。`;

        const reviseResult = await this.provider.generate({ prompt: revisePrompt });
        currentContent = reviseResult.text;
        reviserUsage.promptTokens += reviseResult.usage.promptTokens;
        reviserUsage.completionTokens += reviseResult.usage.completionTokens;
        reviserUsage.totalTokens += reviseResult.usage.totalTokens;
      }
    }

    return {
      content: currentContent,
      auditorUsage,
      reviserUsage,
      usage: {
        promptTokens: auditorUsage.promptTokens + reviserUsage.promptTokens,
        completionTokens: auditorUsage.completionTokens + reviserUsage.completionTokens,
        totalTokens: auditorUsage.totalTokens + reviserUsage.totalTokens,
      },
      warning: 'accept_with_warnings',
      warningCode: 'accept_with_warnings',
    };
  }
```

同时保证 `#auditChapter` 返回 `usage`，若缺则改造其返回类型加入 `usage: result.usage`。

对于 `#generateIntent` 与 `#buildContextCard`，同样追加 `usage` 字段（由 `this.provider.generate/generateJSON` 获得）。注意：`generateJSON` 目前不返回 usage，暂按估算 0 或改造 provider。若 generateJSON 不返回 usage，planner 通道先按 0 写入（保持类型完整）。

- [ ] **Step 5：在 writeNextChapter 对应调用点补打点**

在 `writeNextChapter` 内：

```typescript
      const audited = await this.#auditAndRevise(polished.content!, input, meta.genre);
      this.#trackUsage(input.bookId, input.chapterNumber, 'auditor', audited.auditorUsage);
      this.#trackUsage(input.bookId, input.chapterNumber, 'reviser', audited.reviserUsage);
```

以及在 intent / contextCard 调用后：

```typescript
      const intent = await this.#generateIntent(input, meta.genre);
      this.#trackUsage(input.bookId, input.chapterNumber, 'planner', intent.usage);

      const contextCard = await this.#buildContextCard(input.bookId, input.chapterNumber);
      this.#trackUsage(input.bookId, input.chapterNumber, 'planner', contextCard.usage);
```

若当前 `#generateIntent` / `#buildContextCard` 未返回对象而是 raw record，先将其返回类型改为 `{ data: X; usage?: ... }`，并在所有 caller 处改用 `.data` 字段。

- [ ] **Step 6：typecheck**

Run: `pnpm --filter @cybernovelist/core typecheck`

Expected: 无错误。

- [ ] **Step 7：提交**

```bash
git add packages/core/src/pipeline/runner.ts
git commit -m "feat(core): wire TelemetryLogger into writeNextChapter pipeline"
```

---

## Task 4：PipelineRunner.writeDraft / writeFastDraft / upgradeDraft 通道打点

**Files:**
- Modify: `packages/core/src/pipeline/runner.ts`

- [ ] **Step 1：在 writeDraft 追加 writer 通道打点**

找到 `writeDraft`（约第 347 行）的 `result = await this.provider.generate(...)` 之后追加：

```typescript
      this.#trackUsage(input.bookId, input.chapterNumber, 'writer', result.usage);
```

- [ ] **Step 2：在 writeFastDraft 追加 writer 通道打点**

同样在 `writeFastDraft`（约第 397 行）的 `result = await this.provider.generate(...)` 之后追加：

```typescript
      this.#trackUsage(input.bookId, input.chapterNumber, 'writer', result.usage);
```

- [ ] **Step 3：upgradeDraft 按链路追加打点**

在 `upgradeDraft` 中，对其内部调用的 `#generateDraft` / `#polishScene` / `#auditAndRevise` 追加与 Task 3 Step 5 一致的打点。

- [ ] **Step 4：typecheck**

Run: `pnpm --filter @cybernovelist/core typecheck`

Expected: 无错误。

- [ ] **Step 5：提交**

```bash
git add packages/core/src/pipeline/runner.ts
git commit -m "feat(core): wire TelemetryLogger into writeDraft/writeFastDraft/upgradeDraft"
```

---

## Task 5：PipelineRunner 集成测试（遥测落盘断言）

**Files:**
- Modify: `packages/core/src/pipeline/runner.test.ts`

- [ ] **Step 1：在 runner.test.ts 追加遥测断言测试**

在现有 writeNextChapter 相关测试 `describe` 块末尾追加：

```typescript
  it('writeNextChapter 完成后写入遥测文件并包含各通道 usage', async () => {
    // 前置：使用现有 makeRunner() 工厂（若测试文件已有）
    const runner = makeRunner();
    await runner.initBook({
      bookId: 'book-telemetry',
      title: '遥测测试',
      genre: '都市',
      synopsis: '遥测集成验证',
    });

    const result = await runner.writeNextChapter({
      bookId: 'book-telemetry',
      chapterNumber: 1,
      title: '第一章',
      genre: '都市',
      userIntent: '主角开场',
    });

    expect(result.success).toBe(true);

    const fs = await import('node:fs');
    const path = await import('node:path');
    const telemetryPath = path.join(
      testRoot, // 测试文件内已有的 tmp 根
      'book-telemetry',
      'story',
      'state',
      'telemetry',
      'chapter-0001.json',
    );
    expect(fs.existsSync(telemetryPath)).toBe(true);

    const telemetry = JSON.parse(fs.readFileSync(telemetryPath, 'utf-8')) as {
      channels: Record<string, { totalTokens: number; calls: number }>;
      totalTokens: number;
    };
    // 至少 writer 通道被调用
    expect(telemetry.channels.writer.calls).toBeGreaterThanOrEqual(1);
    expect(telemetry.totalTokens).toBeGreaterThan(0);
  });
```

（若现有测试使用其他命名约定，沿用：`testRoot` 或 `tmpDir` 变量名与 `makeRunner()` 工厂由本文件 beforeEach 注入。打开文件顶部确认引用符合实际。）

- [ ] **Step 2：运行测试**

Run: `pnpm --filter @cybernovelist/core test -- pipeline/runner`

Expected: PASS — 遥测文件存在且 writer.calls ≥ 1。

- [ ] **Step 3：提交**

```bash
git add packages/core/src/pipeline/runner.test.ts
git commit -m "test(core): assert telemetry file written after writeNextChapter"
```

---

## Task 6：Studio core-bridge 暴露 LLMProvider

**Files:**
- Modify: `packages/studio/src/api/core-bridge.ts`

- [ ] **Step 1：在 core-bridge.ts 新增 getStudioLLMProvider**

在 `getStudioPipelineRunner()` 下方（约第 228 行）新增：

```typescript
let llmProvider: LLMProvider | null = null;

export function getStudioLLMProvider(): LLMProvider {
  if (!llmProvider) {
    llmProvider = new DeterministicProvider();
  }
  return llmProvider;
}

export function setStudioLLMProviderForTests(provider: LLMProvider | null): void {
  llmProvider = provider;
}
```

同时在 `resetStudioCoreBridgeForTests` 函数中加入 `llmProvider = null;`（确保测试隔离）。

- [ ] **Step 2：typecheck**

Run: `pnpm --filter @cybernovelist/studio typecheck`

Expected: 无错误。

- [ ] **Step 3：提交**

```bash
git add packages/studio/src/api/core-bridge.ts
git commit -m "feat(studio): expose LLMProvider via getStudioLLMProvider()"
```

---

## Task 7：analytics.ts token-usage 聚合遥测文件

**Files:**
- Modify: `packages/studio/src/api/routes/analytics.ts`

- [ ] **Step 1：引入 TelemetryLogger**

在 [packages/studio/src/api/routes/analytics.ts](packages/studio/src/api/routes/analytics.ts) 顶部新增：

```typescript
import { TelemetryLogger } from '@cybernovelist/core';
```

- [ ] **Step 2：替换 token-usage 端点实现**

将原先返回占位数据的 `/token-usage` 端点（约第 161–181 行）替换为：

```typescript
  // GET /api/books/:bookId/analytics/token-usage
  router.get('/token-usage', (c) => {
    const bookId = c.req.param('bookId')!;
    if (!hasStudioBookRuntime(bookId)) {
      return c.json({ error: { code: 'BOOK_NOT_FOUND', message: '书籍不存在' } }, 404);
    }

    const telemetry = new TelemetryLogger(getStudioRuntimeRootDir()).listBookTelemetry(bookId);

    const perChannel = { writer: 0, auditor: 0, planner: 0, composer: 0, reviser: 0 };
    let totalTokens = 0;

    for (const chapter of telemetry) {
      for (const key of Object.keys(perChannel) as Array<keyof typeof perChannel>) {
        perChannel[key] += chapter.channels[key]?.totalTokens ?? 0;
      }
      totalTokens += chapter.totalTokens;
    }

    const perChapter = telemetry.map((ch) => ({
      chapter: ch.chapterNumber,
      totalTokens: ch.totalTokens,
      channels: {
        writer: ch.channels.writer.totalTokens,
        auditor: ch.channels.auditor.totalTokens,
        planner: ch.channels.planner.totalTokens,
        composer: ch.channels.composer.totalTokens,
        reviser: ch.channels.reviser.totalTokens,
      },
    }));

    return c.json({
      data: {
        totalTokens,
        perChannel,
        perChapter,
      },
    });
  });
```

- [ ] **Step 3：typecheck**

Run: `pnpm --filter @cybernovelist/studio typecheck`

Expected: 无错误。

- [ ] **Step 4：提交**

```bash
git add packages/studio/src/api/routes/analytics.ts
git commit -m "feat(studio): token-usage aggregates from real telemetry files"
```

---

## Task 8：analytics.test.ts 覆盖 token-usage 真实聚合

**Files:**
- Modify: `packages/studio/src/api/routes/analytics.test.ts`

- [ ] **Step 1：替换现有 token-usage 测试**

将 [packages/studio/src/api/routes/analytics.test.ts:154-177](packages/studio/src/api/routes/analytics.test.ts#L154-L177) 的 `describe('GET /api/books/:bookId/analytics/token-usage'` 整块替换为：

```typescript
  describe('GET /api/books/:bookId/analytics/token-usage', () => {
    function writeTelemetry(
      bookId: string,
      chapterNumber: number,
      channels: Partial<Record<'writer' | 'auditor' | 'planner' | 'composer' | 'reviser', number>>,
    ) {
      const root = getStudioRuntimeRootDir();
      const dir = path.join(root, bookId, 'story', 'state', 'telemetry');
      fs.mkdirSync(dir, { recursive: true });
      const padded = String(chapterNumber).padStart(4, '0');

      const allChannels = ['writer', 'auditor', 'planner', 'composer', 'reviser'] as const;
      const fullChannels: Record<string, { promptTokens: number; completionTokens: number; totalTokens: number; calls: number }> = {};
      let total = 0;
      for (const ch of allChannels) {
        const t = channels[ch] ?? 0;
        fullChannels[ch] = {
          promptTokens: Math.floor(t * 0.6),
          completionTokens: Math.floor(t * 0.4),
          totalTokens: t,
          calls: t > 0 ? 1 : 0,
        };
        total += t;
      }

      fs.writeFileSync(
        path.join(dir, `chapter-${padded}.json`),
        JSON.stringify({
          bookId,
          chapterNumber,
          channels: fullChannels,
          totalTokens: total,
          createdAt: new Date().toISOString(),
          updatedAt: new Date().toISOString(),
        }),
      );
    }

    it('返回正确结构当无遥测数据时', async () => {
      const res = await app.request('/api/books/book-001/analytics/token-usage');
      expect(res.status).toBe(200);
      const data = (await res.json()) as {
        data: { totalTokens: number; perChannel: Record<string, number>; perChapter: unknown[] };
      };
      expect(data.data.totalTokens).toBe(0);
      expect(data.data.perChannel.writer).toBe(0);
      expect(Array.isArray(data.data.perChapter)).toBe(true);
      expect(data.data.perChapter.length).toBe(0);
    });

    it('聚合多章节遥测文件并按通道汇总', async () => {
      writeTelemetry('book-001', 1, { writer: 1000, auditor: 200, planner: 100 });
      writeTelemetry('book-001', 2, { writer: 1500, auditor: 300, reviser: 400 });

      const res = await app.request('/api/books/book-001/analytics/token-usage');
      expect(res.status).toBe(200);
      const data = (await res.json()) as {
        data: {
          totalTokens: number;
          perChannel: { writer: number; auditor: number; planner: number; composer: number; reviser: number };
          perChapter: Array<{ chapter: number; totalTokens: number }>;
        };
      };

      expect(data.data.totalTokens).toBe(3500);
      expect(data.data.perChannel.writer).toBe(2500);
      expect(data.data.perChannel.auditor).toBe(500);
      expect(data.data.perChannel.planner).toBe(100);
      expect(data.data.perChannel.reviser).toBe(400);
      expect(data.data.perChannel.composer).toBe(0);
      expect(data.data.perChapter.length).toBe(2);
      expect(data.data.perChapter[0].chapter).toBe(1);
      expect(data.data.perChapter[0].totalTokens).toBe(1300);
    });

    it('book 不存在返回 404', async () => {
      const res = await app.request('/api/books/non-existent/analytics/token-usage');
      expect(res.status).toBe(404);
    });
  });
```

- [ ] **Step 2：运行测试确认通过**

Run: `pnpm --filter @cybernovelist/studio test -- analytics`

Expected: PASS — token-usage 三条测试通过。

- [ ] **Step 3：提交**

```bash
git add packages/studio/src/api/routes/analytics.test.ts
git commit -m "test(studio): token-usage aggregates telemetry files"
```

---

## Task 9：analytics.ts inspiration-shuffle 调用真实 LLM

**Files:**
- Modify: `packages/studio/src/api/routes/analytics.ts`

- [ ] **Step 1：引入 getStudioLLMProvider**

在 import 区追加：

```typescript
import { getStudioLLMProvider } from '../core-bridge';
```

- [ ] **Step 2：替换 inspiration-shuffle 端点实现**

将 [packages/studio/src/api/routes/analytics.ts:460-483](packages/studio/src/api/routes/analytics.ts#L460-L483) 的 `/inspiration-shuffle` 端点替换为：

```typescript
  // POST /api/books/:bookId/analytics/inspiration-shuffle
  router.post('/inspiration-shuffle', async (c) => {
    const bookId = c.req.param('bookId')!;
    if (!hasStudioBookRuntime(bookId)) {
      return c.json({ error: { code: 'BOOK_NOT_FOUND', message: '书籍不存在' } }, 404);
    }

    const index = readIndex(bookId);
    if (!index || index.chapters.length === 0) {
      return c.json({
        data: {
          alternatives: [],
          generationTime: 0,
          available: false,
          reason: 'no_chapters',
        },
      });
    }

    const latest = index.chapters[index.chapters.length - 1];
    const latestContent = readChapterContent(bookId, latest.number);
    if (!latestContent) {
      return c.json({
        data: { alternatives: [], generationTime: 0, available: false, reason: 'no_content' },
      });
    }

    const styles: Array<{
      id: string;
      style: string;
      label: string;
      characteristics: string[];
      promptDirective: string;
    }> = [
      {
        id: 'A',
        style: 'fast_paced',
        label: '快节奏视角',
        characteristics: ['紧凑', '短句', '强推进'],
        promptDirective: '请将章节改写为快节奏、短句为主、强推进情节的版本。',
      },
      {
        id: 'B',
        style: 'emotional',
        label: '细腻情感',
        characteristics: ['情绪深度', '心理刻画', '慢镜头'],
        promptDirective: '请将章节改写为情感细腻、强化心理刻画的版本。',
      },
      {
        id: 'C',
        style: 'contemplative',
        label: '内省冷静',
        characteristics: ['哲思', '节制', '克制'],
        promptDirective: '请将章节改写为冷静内省、节制克制、含哲思的版本。',
      },
    ];

    const provider = getStudioLLMProvider();
    const start = Date.now();

    const results = await Promise.all(
      styles.map(async (s) => {
        try {
          const prompt = `你是一位风格改写师。
${s.promptDirective}

## 原内容（节选）
${latestContent.substring(0, 2000)}

请直接输出改写后的正文（控制在 500 字内）。`;
          const resp = await provider.generate({ prompt });
          return {
            id: s.id,
            style: s.style,
            label: s.label,
            text: resp.text,
            wordCount: resp.text.length,
            characteristics: s.characteristics,
          };
        } catch {
          return {
            id: s.id,
            style: s.style,
            label: s.label,
            text: '',
            wordCount: 0,
            characteristics: s.characteristics,
          };
        }
      }),
    );

    const generationTime = (Date.now() - start) / 1000;

    return c.json({
      data: {
        alternatives: results,
        generationTime,
        available: results.some((r) => r.wordCount > 0),
      },
    });
  });
```

- [ ] **Step 3：typecheck**

Run: `pnpm --filter @cybernovelist/studio typecheck`

Expected: 无错误。

- [ ] **Step 4：提交**

```bash
git add packages/studio/src/api/routes/analytics.ts
git commit -m "feat(studio): inspiration-shuffle calls LLMProvider for 3 style rewrites"
```

---

## Task 10：inspiration-shuffle 测试用 DeterministicProvider 验证真实链路

**Files:**
- Modify: `packages/studio/src/api/routes/analytics.test.ts`

- [ ] **Step 1：替换现有 inspiration-shuffle 测试**

将 [packages/studio/src/api/routes/analytics.test.ts:246-266](packages/studio/src/api/routes/analytics.test.ts#L246-L266) 的 `describe('POST /api/books/:bookId/analytics/inspiration-shuffle'` 替换为：

```typescript
  describe('POST /api/books/:bookId/analytics/inspiration-shuffle', () => {
    it('无章节时返回 available: false', async () => {
      const res = await app.request('/api/books/book-001/analytics/inspiration-shuffle', {
        method: 'POST',
      });
      expect(res.status).toBe(200);
      const data = (await res.json()) as {
        data: { alternatives: unknown[]; available: boolean };
      };
      expect(data.data.available).toBe(false);
      expect(data.data.alternatives.length).toBe(0);
    });

    it('有章节时返回 3 个不同风格的改写', async () => {
      addTestChapter('book-001', 1, '第一章', '林晨站在雨中，内心波涛汹涌。他终于明白了那个秘密。');

      const res = await app.request('/api/books/book-001/analytics/inspiration-shuffle', {
        method: 'POST',
      });
      expect(res.status).toBe(200);
      const data = (await res.json()) as {
        data: {
          alternatives: Array<{ id: string; style: string; label: string; text: string; wordCount: number }>;
          generationTime: number;
          available: boolean;
        };
      };

      expect(data.data.alternatives.length).toBe(3);
      const ids = data.data.alternatives.map((a) => a.id).sort();
      expect(ids).toEqual(['A', 'B', 'C']);
      const styles = data.data.alternatives.map((a) => a.style).sort();
      expect(styles).toEqual(['contemplative', 'emotional', 'fast_paced']);
      expect(data.data.available).toBe(true);
      for (const alt of data.data.alternatives) {
        expect(alt.text.length).toBeGreaterThan(0);
        expect(alt.wordCount).toBeGreaterThan(0);
      }
      expect(typeof data.data.generationTime).toBe('number');
    });
  });
```

- [ ] **Step 2：运行测试**

Run: `pnpm --filter @cybernovelist/studio test -- analytics`

Expected: PASS。

- [ ] **Step 3：提交**

```bash
git add packages/studio/src/api/routes/analytics.test.ts
git commit -m "test(studio): inspiration-shuffle returns 3 real LLM-generated alternatives"
```

---

## Task 11：前端 analytics.tsx 类型对齐

**Files:**
- Modify: `packages/studio/src/pages/analytics.tsx`

- [ ] **Step 1：修正 TokenUsageData 类型与 UI 渲染**

将 [packages/studio/src/pages/analytics.tsx:34-37](packages/studio/src/pages/analytics.tsx#L34-L37) 的 `TokenUsageData` 接口替换为：

```typescript
interface TokenUsageData {
  totalTokens: number;
  perChannel: {
    writer: number;
    auditor: number;
    planner: number;
    composer: number;
    reviser: number;
  };
  perChapter: Array<{
    chapter: number;
    totalTokens: number;
    channels: {
      writer: number;
      auditor: number;
      planner: number;
      composer: number;
      reviser: number;
    };
  }>;
}
```

- [ ] **Step 2：更新 Token Usage 渲染块**

将 [packages/studio/src/pages/analytics.tsx:247-278](packages/studio/src/pages/analytics.tsx#L247-L278) 的 Token Usage 区块内 `tokenUsage.perChapter`（旧的 agent 分组）替换为：

```tsx
      <div className="rounded-lg border bg-card p-6">
        <div className="flex items-center gap-2 mb-4">
          <Zap size={18} className="text-yellow-500" />
          <h2 className="text-lg font-semibold">Token 用量</h2>
        </div>
        <div className="mb-4">
          <InfoCard label="总计" value={tokenUsage?.totalTokens.toLocaleString() || '0'} />
        </div>
        {tokenUsage && (
          <div className="space-y-2">
            {(Object.keys(tokenUsage.perChannel) as Array<keyof typeof tokenUsage.perChannel>).map((agent) => {
              const tokens = tokenUsage.perChannel[agent];
              const max = Math.max(...Object.values(tokenUsage.perChannel), 1);
              const pct = (tokens / max) * 100;
              return (
                <div key={agent} className="flex items-center gap-3">
                  <span className="text-sm w-20 text-muted-foreground">{agent}</span>
                  <div className="flex-1 h-4 bg-secondary rounded-full overflow-hidden">
                    <div
                      className="h-full bg-purple-500 rounded-full transition-all"
                      style={{ width: `${pct}%` }}
                    />
                  </div>
                  <span className="text-sm w-16 text-muted-foreground">
                    {tokens.toLocaleString()}
                  </span>
                </div>
              );
            })}
          </div>
        )}
        {tokenUsage && tokenUsage.totalTokens === 0 && (
          <p className="text-xs text-muted-foreground mt-3">
            尚未产生 Token 消耗。开始写作后将自动汇总。
          </p>
        )}
      </div>
```

- [ ] **Step 3：typecheck**

Run: `pnpm --filter @cybernovelist/studio typecheck`

Expected: 无错误。

- [ ] **Step 4：提交**

```bash
git add packages/studio/src/pages/analytics.tsx
git commit -m "refactor(studio): align analytics.tsx TokenUsageData with new per-channel shape"
```

---

## Task 12：终局验证

**Files:** 无修改

- [ ] **Step 1：跑 core 全量测试**

Run: `pnpm --filter @cybernovelist/core test`

Expected: 全部通过。

- [ ] **Step 2：跑 studio 全量测试**

Run: `pnpm --filter @cybernovelist/studio test`

Expected: 全部通过。

- [ ] **Step 3：跑整体 typecheck**

Run: `pnpm -r typecheck`

Expected: 无错误。

- [ ] **Step 4：手动烟测（可选）**

启动 studio：`pnpm --filter @cybernovelist/studio dev`
打开浏览器访问 `/analytics?bookId=<已有 bookId>`
- 验证 Token 用量区块的 5 个通道数值非全 0（若有写作历史）
- 点击"生成灵感"按钮，验证返回 3 个不同 label 的改写卡片，且 text 非占位文案

- [ ] **Step 5：更新 remediation-plan.md 的 P1.1 状态**

编辑 `docs/Development/remediation-plan.md`，将 P1.1 的验收项旁加上 ✅，例如：

```markdown
- 验收标准：
  - `word-count`、`audit-rate`、`token-usage` 返回真实数据。✅
  - `quality-baseline` 与 `baseline-alert` 反映真实趋势。✅
  - `emotional-arcs` 从真实角色/章节数据生成，而不是固定林晨/苏小雨。✅
```

- [ ] **Step 6：提交**

```bash
git add docs/Development/remediation-plan.md
git commit -m "docs: mark P1.1 analytics realization as complete"
```

---

## Self-Review 备忘

- Task 3 Step 4 对 `#generateIntent` / `#buildContextCard` 的 usage 返回形状假设"加上 usage 字段"，实际实现时可能需要把返回类型从裸对象改为 `{ data, usage }` 并 fan-out 修改 caller。若遇到，将该子变更拆成独立 commit 以控影响面。
- `generateJSON` 当前不回填 usage；planner 通道初期可能落盘为 0，这是已知限制，可在后续迭代补 LLMProvider 估算或让 generateJSON 也返回 usage。
- `DeterministicProvider.generate` 会为 inspiration-shuffle 返回模板文本，在真实 LLM 接入前 shuffle 测试依赖 `estimateUsage` 产生非 0 wordCount。
- 若任何 Task 导致其他既有测试断裂（如 runner 的 usage 聚合断言），优先修测试里的期望而非放弃打点。
